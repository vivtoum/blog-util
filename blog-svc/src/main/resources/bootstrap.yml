spring:
  application:
    name: blog-svc #应用名
  cloud:
    config:
      uri: http://www.vivtoum.tech:1201 #server服务
      name: blog-svc #寻找配置文件(,)隔开是多个配置文件
      profile: single
      label: master
      enabled: true #开启远程配置中心
